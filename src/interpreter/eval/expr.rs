// src/interpreter/eval/expr.rs
//! Expression evaluation.
//! Full implementation next response.

#![allow(dead_code, unused_variables)]

use crate::ast::expressions::Expr;
use crate::interpreter::eval::Interpreter;
use crate::interpreter::value::EvalResult;

pub fn eval_expr<'ast>(interp: &mut Interpreter<'ast>, expr: &Expr<'ast>) -> EvalResult {
    todo!("eval_expr — full implementation next response")
                       }
