// Brought to u by MidManStudio
// display()-catch-all-fix + MEMORY_MODEL.md §9 delivery: cleanup of the
// now-stale method-tier-gating.tar.gz replacements bundle from the previous
// session (already applied, and would otherwise get re-extracted alongside
// this session's bundle, clobbering files both bundles touch — tier/mod.rs
// and type_infer.rs — with its older content).

@CONFIG(
  version  -> "1.0.0",
  features -> "data"
)

@DATA(
  delete_files::
    { path = ".mdix/replacements/method-tier-gating.tar.gz" }
)
