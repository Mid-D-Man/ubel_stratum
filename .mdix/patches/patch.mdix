// Brought to u by MidManStudio
// MEMORY_MODEL.md §8 delivery: cleanup of the now-stale error-reorg.tar.gz
// replacements bundle from the previous session (already applied, and
// would otherwise get re-extracted alongside this session's bundle,
// clobbering files both bundles touch with its older content).

@CONFIG(
  version  -> "1.0.0",
  features -> "data"
)

@DATA(
  delete_files::
    { path = ".mdix/replacements/error-reorg.tar.gz" }
)
